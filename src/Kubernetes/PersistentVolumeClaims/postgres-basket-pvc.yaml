apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-basket-pvc
  namespace: eshop
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: hostpath